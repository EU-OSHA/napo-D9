label: Căutare
display:
  default:
    display_options:
      exposed_form:
        options:
          submit_button: Căutare
      filters:
        search_api_fulltext:
          expose:
            label: Căutare
      title: 'Rezultatele căutării {% if(filters.search_api_fulltext != "") %} pentru  "{{ filters.search_api_fulltext }}" {%endif%}'
      empty:
        area_text_custom:
          content: 'Niciun rezultat'
